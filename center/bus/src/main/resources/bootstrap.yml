server:
  port: ${SERVER_PORT:8071}
spring:
  application:
    name: bus
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:192.168.56.101}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:192.168.56.101}:${REGISTER_PORT:8848}
        file-extension: yml